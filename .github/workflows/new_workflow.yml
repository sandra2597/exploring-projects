name: New Workflow
on:
  push:
    branches-ignore:
      - main
      - dev
      
jobs:
  create-pull-request:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/github-script@v6
      id: set-result
      with:
        script: console.log(context)
